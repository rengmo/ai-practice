# 练习指南

这是一个进行AI相关练习的项目。

## 环境变量
- STYLE_REFERENCE_IMAGE, 参考图片，生成图片的时候都是参考这个地址对应的图片的风格。
    - 突出标题，弱化背景
    - 背景只是一些图案示意图，尽量少文字描述

## 图片生成

生成的图片放到output/images目录下。
除非特殊说明，否则都生成900 × 383 像素的图。
- 头条（首图）：推荐尺寸为 900 × 383 像素（比例为 2.35:1）。
- 次条（多图文）：推荐尺寸为 200 × 200 像素（比例为 1:1）。